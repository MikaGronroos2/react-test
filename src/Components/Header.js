import React from "react";

const Header = () => {
  return (
    <div className="header">
      <p>
        This is the Header, maybe add some links that will take the user to
        places like "Projects" or "Skills"
      </p>
    </div>
  );
};

export default Header;
